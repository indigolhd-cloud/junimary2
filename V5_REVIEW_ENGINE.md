# EngStep v5 — Cloud Review Engine

## 복습 규칙
- 최근 7일/30일 오답: 해당 기간에 한 번이라도 틀린 문제를 중복 없이 표시/출제합니다.
- 누적 오답 횟수: 이후 정답을 맞혀도 과거 오답 기록은 유지됩니다.
- 집중복습: 7일/30일/집중복습 중 다시 틀린 문제를 자동 분류합니다.
- 학습완료: 오답이 있었던 문제를 이후 3회 연속 맞히면 학습완료 상태로 표시합니다.
- 간격 복습: 연속 정답에 따라 기본 재검토 간격을 1 → 2 → 4 → 7 → 14일로 늘립니다.
- 오늘의 복습: 하루 20문제. 집중복습/누적 오답을 우선하고 최근 단어·문법 정답률에 따라 유형 비중을 조정합니다.
- 오늘의 완료 수는 문제 ID 기준으로 중복 계산하지 않습니다.

## 클라우드
복습 상태 자체를 별도 행으로 덮어쓰지 않고 `attempts` 전체 이력에서 계산합니다. 따라서 같은 계정으로 동기화된 기기는 동일한 풀이 이력을 기준으로 복습 상태를 계산합니다.
